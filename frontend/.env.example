# Copy this file to .env and add your actual Cloudinary credentials
# You can get these from your Cloudinary dashboard: https://cloudinary.com/console

# Your Cloudinary cloud name
REACT_APP_CLOUDINARY_CLOUD_NAME=your_cloud_name_here

# Your Cloudinary upload preset (must be configured as UNSIGNED in your Cloudinary settings)
# To create an unsigned preset:
# 1. Go to Settings > Upload in your Cloudinary dashboard
# 2. Scroll down to "Upload presets"
# 3. Click "Add upload preset"
# 4. Set "Signing Mode" to "Unsigned"
# 5. Give it a name (e.g., "ml_default")
# 6. Configure any other settings as needed
# 7. Save the preset
REACT_APP_CLOUDINARY_UPLOAD_PRESET=your_upload_preset_here

# Example values (replace with your actual values):
# REACT_APP_CLOUDINARY_CLOUD_NAME=demo
# REACT_APP_CLOUDINARY_UPLOAD_PRESET=ml_default

# Note: For frontend uploads, you do NOT need the API secret
# The API secret is only needed for server-side (signed) uploads
